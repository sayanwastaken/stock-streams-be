# no multi stage build here, dev image is for local development only
FROM node:22-alpine
WORKDIR /app

# Install only once; cached unless package*.json changes
COPY package*.json ./
RUN npm ci

# App code will be bind-mounted in compose
CMD ["npm", "run", "start:dev"]
